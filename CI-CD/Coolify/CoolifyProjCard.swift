import SwiftUI

struct CoolifyProjCard: View {
    private let proj: CoolifyProject
    
    init(_ proj: CoolifyProject) {
        self.proj = proj
    }
    
    var body: some View {
        Text("Hello, World!")
    }
}
