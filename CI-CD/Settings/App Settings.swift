import SwiftUI

struct AppSettings: View {
    var body: some View {
        List {
            Text("Hello, World!")
        }
        .navigationTitle("Settings")
    }
}

#Preview {
    AppSettings()
}
